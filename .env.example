# ── LLM API（OpenAI 相容 Chat Completions，必填）──────────
# 貼「v1 base」（如下範例）或貼整段「完整 completions 端點」
# （即 .../v1/chat/completions）皆可，程式會自動正規化、不會重複組出
# .../chat/completions/chat/completions。
LLM_BASE_URL=https://your-llm-gateway.example.com/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=your_model_here
LLM_VERIFY=false
# 等待 gateway 回應的秒數上限（read timeout）。預設 120 秒。
# LLM_TIMEOUT=120
# system prompt 傳遞模式，三階梯（依序嘗試）：
#   system      獨立 system role 訊息（預設）
#   inline      併入第一則 user 訊息文字，供會忽略 system 訊息的 gateway 使用
#   single_turn 最後手段：把角色指令＋對話歷史＋本次輸入攤平成恰好一則 user
#               訊息，供完全不支援多輪（只轉發最後一則訊息）的 gateway 使用
# 若 GET /api/llm/health 回傳 system_prompt_honored=false 或 history_honored=false，
# 打 GET /api/llm/diagnose 取得建議設定。
# LLM_SYSTEM_MODE=system

# messages 的 content 形態：parts（預設，維持現行為，[{"type":"text","text":...}]）
# 或 string（content 直接放字串）。部分 gateway 只對字串 content 做完整多輪
# 處理、對 parts 陣列只取最後一則；若 /api/llm/diagnose 建議 string，改設此值。
# LLM_CONTENT_FORMAT=parts

# ── Flask ──────────────────────────────────────────────
# 非 debug 模式下若 SECRET_KEY 仍是這個預設值，python app.py 啟動時會報錯退出。
SECRET_KEY=change-me-to-a-random-32-char-string
# 設為 1/true 開啟 debug 模式（自動重載 + debugger）。預設關閉。
FLASK_DEBUG=false
# python app.py 綁定的位址，預設 127.0.0.1。對外服務可設為 0.0.0.0（建議搭配反向代理）。
HOST=127.0.0.1

# ── 儲存後端（選填）────────────────────────────────────
# 設定後，Session 改存於 PostgreSQL；未設定則 fallback 至 data/*.json
# DATABASE_URL=postgresql://user:pass@host:5432/sql_agent
# DATA_DIR=data   # JSON 模式的資料目錄（預設 data/）

# ── 變更審批（選填）────────────────────────────────────
# 核准/駁回 DB Agent 提案的結構變更所需的共享密鑰。未設定時 approve/reject 端點一律回 403。
# ADMIN_TOKEN=change-me-to-a-random-secret
