# ── LLM API（OpenAI 相容 Chat Completions，必填）──────────
LLM_BASE_URL=https://your-llm-gateway.example.com/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=your_model_here
LLM_VERIFY=false

# ── Flask ──────────────────────────────────────────────
FLASK_ENV=production
SECRET_KEY=change-me-to-a-random-32-char-string

# ── 儲存後端（選填）────────────────────────────────────
# 設定後，Session 改存於 PostgreSQL；未設定則 fallback 至 data/*.json
# DATABASE_URL=postgresql://user:pass@host:5432/sql_agent
# DATA_DIR=data   # JSON 模式的資料目錄（預設 data/）

# ── 變更審批（選填）────────────────────────────────────
# 核准/駁回 DB Agent 提案的結構變更所需的共享密鑰。未設定時 approve/reject 端點一律回 403。
# ADMIN_TOKEN=change-me-to-a-random-secret
